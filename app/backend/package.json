{
  "name": "rxmantra-backend",
  "version": "1.0.0",
  "private": true,
  "description": "RxMantra API backend — upload validation + proxy to the Python ML service.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "multer": "^1.4.5-lts.1"
  }
}
